[package]
name = "dry-run-derive"
version = "0.1.0"
edition = "2021"

[dependencies]
syn = { version = "2.0", features = ["derive"] }
quote = "1.0"
proc-macro2 = "1.0"
clap = { version = "4.5.4", features = ["derive", "wrap_help", "color", "env"] }

[lib]
proc-macro = true

[features]
default = []
derive = []  # Add a derive feature here for users
